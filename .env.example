# Скопируй в .env.local для локальной разработки или на сервер для продакшена.
# Не коммить файл с реальными значениями.

# --- Заявки с сайта (обязательно для приёма заявок) ---
# Telegram: создай бота через @BotFather, получи токен. Chat ID — ID чата/группы (можно узнать через @userinfobot).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Cloudflare Turnstile: если заданы — форма требует прохождение капчи.
# Dashboard: https://dash.cloudflare.com/ → Turnstile → виджет biznespark.
# В Hostname Management обязательно добавь ОБА хоста (каждый отдельно):
#   - корень: xn--14-6kcdulgtyvmj.xn--p1ai
#   - www:     www.xn--14-6kcdulgtyvmj.xn--p1ai
# Без обоих на проде виджет не отобразится (ошибка «Domain not authorized»).
# Site key — при сборке (NEXT_PUBLIC_*), Secret key — только на сервере (runtime).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- Вебхук Profitbase → Telegram (опционально) ---
# Секрет для проверки входящих вебхуков от Profitbase.
# В кабинете Profitbase укажи URL: https://ваш-домен/api/profitbase-webhook?secret=ЗНАЧЕНИЕ_НИЖЕ
PROFITBASE_WEBHOOK_SECRET=

# --- Виджет подбора квартир Profitbase (опционально) ---
# Без них виджет не показывается. Значения — из кабинета Profitbase.
NEXT_PUBLIC_PB_HOST=
NEXT_PUBLIC_PB_DOMAIN=
NEXT_PUBLIC_PB_ACCOUNT_ID=
NEXT_PUBLIC_PB_REFERRER=
NEXT_PUBLIC_PB_API_KEY=
